# Meridian tests

The test suite covers application contracts, financial calculations, persistence, integration boundaries, and browser behavior. Synthetic fixtures support isolated checks without using live financial records.

A passing isolated test does not establish that every provider integration or complete user journey is release-ready. See the [project overview](../README.md) for product status.
