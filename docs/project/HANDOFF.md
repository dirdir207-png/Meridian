# HANDOFF — read this first, then the governing docs it names

Generated by `scripts/generate_handoff.py` from the governing documents. **Do not edit by hand** — a hand-edited handoff is a session summary wearing the authority of the plan, which is the failure this file exists to prevent.

- Generated: `2026-09-21T04:57:59+00:00`
- Commit: `4a067c8942e7c10d8345c6788c7610c29630a165` on `feat/meridian-implementation` — **3 tracked path(s) modified, uncommitted**

**Uncommitted tracked changes — finish or revert before relying on this handoff:**
- `M docs/project/HANDOFF.md`
- ` M docs/project/MERIDIAN_ROADMAP.md`
- ` M docs/project/session-emergent.json`

> **MAINTAINER'S OBLIGATION.** At the end of a session, or at any context checkpoint:
> regenerate this file (`scripts/generate_handoff.py`), record anything new in
> `docs/project/session-emergent.json`, and run `scripts/roadmap_handoff_check.py`.
> **If this file is older than the newest commit, the previous session did not do that**
> — regenerate it before trusting the 'IN FLIGHT' and 'RECENT COMMITS' sections, then
> check `session-emergent.json` for anything that was never recorded at all.

## 1. BASIS — the sources this was derived from, by hash

| Source | sha256 (first 16) |
|---|---|
| `AGENTS.md` | `9bda355ba495d625` |
| `docs/project/MERIDIAN_ROADMAP.md` | `ea3da37c21556d8a` |
| `docs/project/MERIDIAN_DECISIONS.md` | `0389f404f3ec9218` |
| `docs/project/MERIDIAN_OS_TASKS.json` | `65b1f2ef6fe89c10` |

If a hash above differs from the file you are reading, this handoff is **stale** and must be regenerated before it is relied on (`--check` detects exactly this).

## 2. THE ASSIGNED ORDER (quoted from the roadmap, not paraphrased)

> this line used to ask for the shared dated-event model first. That was delivered, so the sequence starts one step later: close Track D's remainder (`OS-038` design gaps, `OS-049` browser baseline), then build **Track I.1** — the envelope *and its permissions*, with a test per role proving it cannot reach a provider write path — then one useful, evidence-backed role (I.2). In parallel, the Crew funding-math arc has reached its measurement phase (`OS-058`, the 2026-10-02 event) and the reserve-versus-amount gap display (`OS-060`) is the next product slice. The council stays high priority, with each added role proving its value on a real user task.

## 3. IN FLIGHT — open work by track

Every unfinished task declares a track; `scripts/roadmap_handoff_check.py` enforces it.

### `design-fidelity`

- `OS-038` [open/medium] Reconcile the remaining concept-vs-current gaps (all four workspaces)
- `OS-065` [ready/medium] Settings visual parity is unverifiable: the isolated preview serves only 1 of its 5 sect  _emergent — not in roadmap_

### `audit`

- `OS-001` [ready/critical] Reconcile current implementation and select first vertical slice  _emergent — not in roadmap_

### `track-C-V1`

- `OS-067` [in_progress/high] Evidence is stale, calendar is wired to nothing, and the blob store is empty so no docum  **OWNER GATE**
- `OS-068` [open/medium] Plan-page invoice linking is deliberately loose: give bill-invoice matching its own prec

### `track-C-V3`

- `OS-059` [ready/medium] Expose Crew's Autopilot settings, the sweep threshold and the pocket-transfer allocation

### `track-C-V4`

- `OS-070` [open/medium] Trial watch: surface a converting trial BEFORE it charges

### `track-C-V5`

- `OS-061` [ready/medium] Draft owner-requested bill restructuring -- new bills, one-time modifications, and resto  _emergent — not in roadmap_
- `OS-069` [open/medium] Detect charges the ledger sees but Meridian does not model -- recurring, and newly-appea

### `track-C-funding-math`

- `OS-058` [ready/high] Measure the 2026-10-02 funding event: confirm the owner's funding model and settle which

### `track-I-on-V2-V3`

- `OS-063` [open/high] Constraint-driven adjustment planning: from an unforeseen cost to one validated plan of 
- `OS-064` [open/medium] Read-only inbound bill-notice ingestion (email/statement) as planning evidence — trust b  _emergent — not in roadmap_

### `virgil-controlled-operations`

- `VIRGIL-A3` [blocked/medium] Virgil-to-Meridian proposal bridge  **OWNER GATE**

### `virgil-device-actions`

- `VIRGIL-A2` [blocked/medium] Typed deterministic iPhone actions beginning with Apple Maps  **OWNER GATE**

### `virgil-expansion`

- `VIRGIL-A5` [blocked/low] Rich conversational Virgil client and capability expansion  **OWNER GATE**

### `virgil-foundation`

- `VIRGIL-A0` [ready/high] Approve Virgil contracts and establish the compatible iOS toolchain  **OWNER GATE**

### `virgil-proactive`

- `VIRGIL-A4` [blocked/medium] Proactive Virgil and bounded long-running Harness tasks  **OWNER GATE**

### `virgil-reactive`

- `VIRGIL-A1` [blocked/high] Authenticated read-only Virgil voice vertical slice  **OWNER GATE**

## 4. WAITING ON THE OWNER

- `OS-067` — Evidence is stale, calendar is wired to nothing, and the blob store is empty so no document open
- `VIRGIL-A0` — Approve Virgil contracts and establish the compatible iOS toolchain
- `VIRGIL-A1` — Authenticated read-only Virgil voice vertical slice
- `VIRGIL-A2` — Typed deterministic iPhone actions beginning with Apple Maps
- `VIRGIL-A3` — Virgil-to-Meridian proposal bridge
- `VIRGIL-A4` — Proactive Virgil and bounded long-running Harness tasks
- `VIRGIL-A5` — Rich conversational Virgil client and capability expansion

## 5. SESSION-EMERGENT — arose in a session, NOT planned, NOT authorised

Recorded in `docs/project/session-emergent.json`. These are ideas and findings, never assignments. **Promotion to planned work is an owner decision recorded in the ledger; appearing here promotes nothing.**

- **Plan-page invoice linking is loose (OS-068)** — `plan.py::_bill_invoice_evidence` asks 'is this a bill email?', which cannot be checked, so it leaks — observed false positives on Verizon, Xfinity and Eversource. Tightening must not starve charge matching, which needs the card alerts the invoice gate currently rejects. · ledger: `OS-068`
- **Detect unmodelled recurring charges by starting from the CHARGE, not a sender filter (OS-069)** — An enumerated filter must know the answer in advance, which is why 28 bill-like senders had no coverage. A recurrence pass over the ledger found 'fruitful membership' at $48/mo that no email keyword had surfaced. Proposes only; never auto-creates a commitment. · ledger: `OS-069`
- **Trial watch — surface a converting trial before it charges (OS-070)** — Owner-stated intent ('we are building a free trial canceler'). The ledger already holds a converted trial: Unifyed.ai Trial -39.99. Depends on V4's suppression lifecycle or it becomes the alert storm V4 is gated on. · ledger: `OS-070`
- **18 legacy evidence links from the retired amount-only matcher are demonstrably wrong** — A $9.99 marketing email is linked to charges 55/59/84, and one charge has the same receipt linked three times. Harmless while nothing reads them; misleading the moment a surface displays transaction links. · not promoted
- **The calendar connector remains wired to nothing — the open half of OS-067** — Referenced only for its OAuth scope constant while a stored token sits unused. This is the third of the three things the owner originally reported, and the only part of OS-067 still outstanding. NOT parked: it is in-flight work on the critical path. · ledger: `OS-067`
- **A harness-mediated Gmail ingestion service is possible but NOT built** — D-016 keeps the avenue available: the app cannot call Composio today, but a scheduled agent-driven sweep is architecturally legitimate. Not authorised by D-016 and would need its own decision, operational footprint, and honest liveness statement. Meridian must keep working without it. · not promoted

## 6. RECENT COMMITS IN THIS LINE

- `4a067c8 fix(handoff): anchor staleness on the handoff's own commit, not HEAD`
- `0ffb39f fix(handoff): compare normalised lines so --check cannot cry stale wrongly`
- `43e4a96 docs(process): generate the session handoff from the governing docs, and enforce the reconciliation`
- `25609e2 docs(process): require every continuation to reconcile session momentum against project state`
- `1b095b5 docs(decisions): D-016 amendment — the surface includes outside tools; the boundary is AUTHORITY, not mechanism`
- `339888f docs(decisions): D-016 — the harness agent, tools and MCPs are a sanctioned extension surface (OS-067)`
- `d7b926d fix(charge): require a corroborating date, so one receipt yields one link (OS-067)`
- `29bd2fc feat(charge): match evidence to the charge it documents, on a verifiable basis (OS-067)`
- `c8d6d69 docs(ledger): record the owner decision not to backfill the 729 content-less rows (OS-067)`
- `3a70ad5 fix(ingest): never record a row whose content was not stored (OS-067)`
- `38763e0 fix(evidence): give a browser a readable page instead of raw JSON when content is missing (OS-067)`
- `4277400 feat(icloud): enforce read-only in code after an IMAP probe wrote to the owner's inbox (OS-067)`

## 7. READ DEEPER — where the facts actually live

- `docs/project/MERIDIAN_ROADMAP.md` — the trajectory and its stated next move
- `docs/project/MERIDIAN_OS_TASKS.json` — every task, its track, gates and limits
- `docs/project/MERIDIAN_DECISIONS.md` — binding decisions; the newest governs
- `docs/project/CURRENT_STATUS.md` — the running record of what was verified
- `docs/project/AGENT_COORDINATION.md` — the claims table — add a row before editing
- `AGENTS.md` — the rules, including the continuity and handoff contracts

**Before acting:** run `scripts/roadmap_handoff_check.py` and reconcile session momentum against project state, per AGENTS.md. If this handoff and the docs disagree, the docs win.
