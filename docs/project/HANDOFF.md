# HANDOFF — read this first, then the governing docs it names

Generated by `scripts/generate_handoff.py` from the governing documents. **Do not edit by hand** — a hand-edited handoff is a session summary wearing the authority of the plan, which is the failure this file exists to prevent.

- Generated: `2026-09-25T13:05:22+00:00`
- Commit: `0585b9a610043322b92b29c8c8b511275819865f` on `feat/meridian-implementation` — working tree clean

> **MAINTAINER'S OBLIGATION.** At the end of a session, or at any context checkpoint:
> regenerate this file (`scripts/generate_handoff.py`), record anything new in
> `docs/project/session-emergent.json`, and run `scripts/roadmap_handoff_check.py`.
> **If this file is older than the newest commit, the previous session did not do that**
> — regenerate it before trusting the 'IN FLIGHT' and 'RECENT COMMITS' sections, then
> check `session-emergent.json` for anything that was never recorded at all.

## 1. BASIS — the sources this was derived from, by hash

| Source | sha256 (first 16) |
|---|---|
| `AGENTS.md` | `7a54c46ce0c6aa56` |
| `docs/project/MERIDIAN_ROADMAP.md` | `b5d429d3d5c3240b` |
| `docs/project/MERIDIAN_DECISIONS.md` | `df35c0a7a59d1466` |
| `docs/project/MERIDIAN_OS_TASKS.json` | `6e14b468b8b5b07f` |

If a hash above differs from the file you are reading, this handoff is **stale** and must be regenerated before it is relied on (`--check` detects exactly this).

## 2. THE ASSIGNED ORDER (quoted from the roadmap, not paraphrased)

> this line used to ask for the shared dated-event model first. That was delivered, so the sequence starts one step later: close Track D's remainder (`OS-038` design gaps, `OS-049` browser baseline), then build **Track I.1** — the envelope *and its permissions*, with a test per role proving it cannot reach a provider write path — then one useful, evidence-backed role (I.2). **I.1 is recorded as `OS-072`** (added 2026-09-21): the track table in this section named I.1 while the ledger carried no task for it, and `scripts/roadmap_handoff_check.py` still reconciled, because its scope covers the tasks it knows about rather than every row of that table. Naming the id here makes the drift checkable, the same way the evidence-lane entry below was made enforceable. In parallel, the Crew funding-math arc has reached its measurement phase (`OS-058`, the 2026-10-02 event) and the reserve-versus-amount gap display (`OS-060`) is the next product slice. The council stays high priority, with each added role proving its value on a real user task.

## 3. IN FLIGHT — open work by track

Every unfinished task declares a track; `scripts/roadmap_handoff_check.py` enforces it.

### `design-fidelity`

- `OS-038` [open/medium] Reconcile the remaining concept-vs-current gaps (all four workspaces)
- `OS-082` [open/high] Adopt the concept's medallion treatment across the app, not just the hub  _emergent — not in roadmap_
- `OS-088` [open/medium] Give the Plan bill rows the concept's medallion, which needs category data that does not  _emergent — not in roadmap_
- `OS-097` [open/medium] Five browser guards fail in this environment, and they are PRE-EXISTING (attribution pro  _emergent — not in roadmap_

### `track-C-V1`

- `OS-067` [in_progress/high] Evidence lane: two of three parts done — the calendar half is BUILT but never OBSERVED (  **OWNER GATE**
- `OS-068` [open/medium] Plan-page invoice linking is deliberately loose: give bill-invoice matching its own prec

### `track-C-V3`

- `OS-059` [ready/medium] Expose Crew's Autopilot settings, the sweep threshold and the pocket-transfer allocation

### `track-C-V4`

- `OS-070` [open/medium] Trial watch: surface a converting trial BEFORE it charges

### `track-C-V5`

- `OS-061` [ready/medium] Draft owner-requested bill restructuring -- new bills, one-time modifications, and resto  _emergent — not in roadmap_
- `OS-069` [open/medium] Detect charges the ledger sees but Meridian does not model -- recurring, and newly-appea

### `track-C-funding-math`

- `OS-058` [ready/high] Measure the 2026-10-02 funding event: confirm the owner's funding model and settle which

### `track-I-envelope`

- `OS-074` [in_progress/high] I.3 — council mechanics: the Skeptic, the council runner, and the rule that disagreement

### `track-I-on-V2-V3`

- `OS-063` [open/high] Constraint-driven adjustment planning: from an unforeseen cost to one validated plan of 
- `OS-064` [open/medium] Read-only inbound bill-notice ingestion (email/statement) as planning evidence — trust b  _emergent — not in roadmap_

### `track-I-surface`

- `OS-076` [open/high] Investigator customer-facing surface with editable question, owner context and related e  **OWNER GATE** _emergent — not in roadmap_

### `virgil-controlled-operations`

- `VIRGIL-A3` [blocked/medium] Virgil-to-Meridian proposal bridge  **OWNER GATE**

### `virgil-device-actions`

- `VIRGIL-A2` [blocked/medium] Typed deterministic iPhone actions beginning with Apple Maps  **OWNER GATE**

### `virgil-expansion`

- `VIRGIL-A5` [blocked/low] Rich conversational Virgil client and capability expansion  **OWNER GATE**

### `virgil-foundation`

- `VIRGIL-A0` [ready/high] Approve Virgil contracts and establish the compatible iOS toolchain  **OWNER GATE**

### `virgil-proactive`

- `VIRGIL-A4` [blocked/medium] Proactive Virgil and bounded long-running Harness tasks  **OWNER GATE**

### `virgil-reactive`

- `VIRGIL-A1` [ready/high] Authenticated read-only Virgil voice vertical slice  **OWNER GATE**

### `information-architecture`

- `OS-104` [ready/high] Payday and funding are one mechanism described twice: one cadence, no separate funding s  _emergent — not in roadmap_

### `track-D-surfaces`

- `OS-084` [open/high] Bring the management routes onto the owner-direct write router
- `OS-085` [open/high] Retool the Connections section to match the rest of the app's visual language  _emergent — not in roadmap_
- `OS-086` [open/high] Research image-generation tooling so artwork stops depending on Astra  _emergent — not in roadmap_
- `OS-112` [open/medium] Safe to Spend: copy Crew's gear -- choose which pockets count toward the figure  _emergent — not in roadmap_
- `OS-113` [open/high] Identify the spend pocket from Crew's own SELECTION, not from its name  _emergent — not in roadmap_

## 4. WAITING ON THE OWNER

- `OS-067` — Evidence lane: two of three parts done — the calendar half is BUILT but never OBSERVED (the dail
- `OS-076` — Investigator customer-facing surface with editable question, owner context and related evidence
- `VIRGIL-A0` — Approve Virgil contracts and establish the compatible iOS toolchain
- `VIRGIL-A1` — Authenticated read-only Virgil voice vertical slice
- `VIRGIL-A2` — Typed deterministic iPhone actions beginning with Apple Maps
- `VIRGIL-A3` — Virgil-to-Meridian proposal bridge
- `VIRGIL-A4` — Proactive Virgil and bounded long-running Harness tasks
- `VIRGIL-A5` — Rich conversational Virgil client and capability expansion

## 5. SESSION-EMERGENT — arose in a session, NOT planned, NOT authorised

Recorded in `docs/project/session-emergent.json`. These are ideas and findings, never assignments. **Promotion to planned work is an owner decision recorded in the ledger; appearing here promotes nothing.**

- **Plan-page invoice linking is loose (OS-068)** — `plan.py::_bill_invoice_evidence` asks 'is this a bill email?', which cannot be checked, so it leaks — observed false positives on Verizon, Xfinity and Eversource. Tightening must not starve charge matching, which needs the card alerts the invoice gate currently rejects. · ledger: `OS-068`
- **Detect unmodelled recurring charges by starting from the CHARGE, not a sender filter (OS-069)** — An enumerated filter must know the answer in advance, which is why 28 bill-like senders had no coverage. A recurrence pass over the ledger found 'fruitful membership' at $48/mo that no email keyword had surfaced. Proposes only; never auto-creates a commitment. · ledger: `OS-069`
- **Trial watch — surface a converting trial before it charges (OS-070)** — Owner-stated intent ('we are building a free trial canceler'). The ledger already holds a converted trial: Unifyed.ai Trial -39.99. Depends on V4's suppression lifecycle or it becomes the alert storm V4 is gated on. · ledger: `OS-070`
- **18 legacy evidence links from the retired amount-only matcher are demonstrably wrong** — A $9.99 marketing email is linked to charges 55/59/84, and one charge has the same receipt linked three times. Harmless while nothing reads them; misleading the moment a surface displays transaction links. · not promoted
- **The daily calendar TRIGGER is chosen but not wired — the last open piece of OS-067** — The app side is BUILT and tested (migration 026, meridian/calendar_context.py, scripts/calendar_context_ingest.py) and the connector is no longer orphaned: calendar now goes through Composio as its only adapter, by owner decision. What does NOT exist is the once-a-day trigger. The fetch needs a Composio call, which only the harness can make today; the app has no Composio client, and giving it a raw Composio credential is its own decision rather than a detail. Nothing may describe calendar as being polled until that is decided. Honest liveness already exists: the store reports the newest observed event and reads STALE rather than healthy when the feed stalls. · ledger: `OS-067`
- **A harness-mediated GMAIL ingestion service is still possible but NOT built** — D-016 keeps the avenue available and the owner OPENED it on 2026-09-21 for CALENDAR read-only context only (Composio is the calendar's sole adapter) -- and explicitly NOT for mail: 'Two different mechanisms for email and calendar.' So the calendar half is now authorised and built; the mail half described here remains unbuilt and needs its own decision, operational footprint, and honest liveness statement. Meridian must keep working without either. · not promoted
- **Desktop Settings: the hub renders inside the 210px rail with heavy text wrapping** — Promoted rather than left parked, because the owner made the decision this entry said it needed. At >=901px the eight hub rows and their group banners sat in the narrow settings rail and wrapped onto four or five lines, while the main column held only an intro paragraph; the 2026-09-18 concept is a PHONE frame and specified no desktop treatment, so inventing one was a design decision rather than a bug fix. Owner's decision, 2026-09-21: 'Promote the hub into the main column at desktop.' Folded into OS-081 together with the reachability defect found the same day -- every Settings link in the product points at ?section=connections, so the hub had no entry point at all. · ledger: `OS-081`
- **Investigator metadata-only model payload needs meaningful source facts for the user-facing surface** — Inspected EvidenceBoundRole._prompt_payload sends IDs/provenance/time, not document facts. Reuse the existing evidence extraction/links and coordinate backend changes rather than treating a new input box as a useful investigation. Owner asked to preserve cross-references. · ledger: `OS-076`
- **Virgil's panel summons the soft keyboard on open and leaves a black screen on close** — Owner-reported from the live app on his phone: the keyboard occludes the full-height sheet when the panel opens, and closing leaves a black or partially-black screen. advisor_fab.js:99 focuses the composer inside the open branch; :410-411 reopens the panel on every page load when localStorage 'sc_advisor_open' is '1', so the keyboard can appear with no user action at all; and the close branch (:103-116) delegates to shell.closeSheet() or tears down the panel itself, after which the page behind is still dark. PROMOTED to OS-080 so it is tracked rather than remembered. Deliberately NOT fixed in the session that found it, at the owner's instruction -- 'keep this in mind, don't let it derail'. · ledger: `OS-080`
- **The floating `Ask Virgil` advisor control overlaps the right end of Plan's new full-width `Add a bill or goal` button at 420x912** — Observed in the governed OS-089 capture after the owner's full-width button shipped (artifacts/plan-mobile-concept-alignment-2026-09-23/after/plan-mobile-air-*-viewport.png): the shell's inline advisor chip floats near the bottom-right of the canvas and sits over the last ~90px of the primary action. The button stays tappable across most of its width, so this is a legibility and target-area concern rather than a blocked action, and it is NOT introduced by this slice -- the same chip overlapped the bill rows before, and the shell's own comments record that its position was already tuned once to stop it occluding this control on first paint. Deliberately NOT fixed here: the chip is a shell-level element shared by every workspace, so moving it is a design decision about the shell, not a Plan fidelity fix, and OS-089's scope was explicitly the Plan surface. Recorded rather than remembered, and not an assignment. · not promoted
- **Today's event ticket offers a dead-end pair: 'View bill' opens the Plan workspace, and the ticket always says no evidence is attached (OS-093)** — Two defects confirmed by reading the code, reported together by the owner from his phone ('it says no evidence is attached to this event and has a view bill link, the link just brings you to the plan page'). Every bill event's `detailHref` is the literal `/meridian?workspace=plan` (`meridian/services/dial.py:440`, and the same literal at :487 and :534), so the control labelled 'View bill' (`dial.js:1125`) delivers a workspace landing page rather than the bill; and `evidenceIds` is a hard-coded empty list (`dial.py:439`, :486), so `dial.js:1118` states 'No evidence is attached to this event' for a bill even where Plan already holds invoice evidence for it. The destination is a product decision rather than a bug fix: a deep link that focuses the bill, an in-place sheet, or leaving Plan as the landing page and rewording the control so it stops promising a bill -- the last is legitimate and free, and choosing it is the owner's call. · ledger: `OS-093`
- **The reported 'double ticket' layout problem on Today's Eversource bill was NOT reproduced, so it is recorded rather than fixed** — The owner reported it in the same message as OS-093: 'the double ticket eversource bill looks messy where the text is placed, overlapping with the line between the two tickets'. A synthetic model shaped like the reported case -- an Eversource bill with a stated funding schedule (the long '$29.89/event, Crew's own estimate, due Sep 20 for Veterans Home' note), no evidence attached, a detailHref, plus a coexisting 'Eversource Payment Arrangement' bill in the same horizon -- measured ZERO text-box overlaps, ZERO nodes escaping the ticket's own border box, and exactly ONE `.obs-evidence-ticket` on the page, at 420x912 and 1440x900 (tmp/probe_today_ticket.py). The first fixture attempt used wrong field names (`perEventAmount`/`dueDate` instead of `contribution`/`deadline`) and rendered none of the long text, so it was re-measured rather than reported. Not reproducing a defect is not evidence that it is absent, and guessing at a visual defect is how a fix lands on the wrong element -- so this half needs the owner's screenshot, or the date and bill he had selected. · ledger: `OS-093`
- **The Plan page Rules and Crew tabs are effectively unstyled and have no governing concept, so they need a NEW design rather than a fidelity pass** — Owner, 2026-09-24: "the rules and crew tab of the plan page have almost no styling. I do not believe there is a previous concept, so it is one you will have to design." Every other visual task in this lane is a fidelity pass against a governing record (D-004). These two panes are the first surface where the owner has authorised a NEW composition, which changes the workflow: the design must be proposed and approved BEFORE implementation, and the owner's stated absence of a concept must be verified against design/ first rather than assumed. Read-only panes; no financial behaviour or authority is involved. · ledger: `OS-102`
- **The owner named four Accounts aesthetic traits; three are measured fidelity deltas, and the fourth changes the glyph system and owes a decision** — Owner, 2026-09-24: "Accounts aesthetic -- larger ticket, more diverse icons, no outer box on accounts, connected dotted line flowing account-to-account on the left." Three of the four are differences between the app and the governing concept 04, so they are measurable and were fixed by measurement. "More diverse icons" is not a delta: the concept draws three emblems for three purpose BUCKETS, the payload keys a glyph on the financial ROLE (`_role("pocket")` resolves to `other`), and the delivered design/investigator-medallions-2026-09-21/ artwork states its own boundary -- "do not use artwork as an account-type classifier", and place the Wi-Fi mark only where the mapping is deliberately accepted. Two forks, different artwork, one needing the owner's acceptance, so the owner picks before anything is drawn. · ledger: `OS-103`
- **Payday and funding are one mechanism described twice: the Settings Cadence card reads the learned pattern while the rest of the app prefers Crew's paycheck record, and there is no way to set a payday at all** — Owner, 2026-09-24: "payday is the funding mechanism, so the payday and when it lands is the cadence, this isn't consistent. It still says no cadence detected in places. Funding the way the app describes separate from payday is per bill and doesn't need a separate setting or section." Verified at both layers: services/payday.py:87 builds `pattern` from recognized transactions only while Crew's plans ride in the same payload, and payday.js:54-64 renders the summary from `pattern` alone, so the card can say 'Not recognized' beside a Crew cadence the same page lists below; the card's own instruction ('Add or confirm your payday timing') has no control behind it, because /settings/payday is GET-only and no payday write exists anywhere; and Settings carries a two-mode copy of the four-mode per-commitment funding editor that Plan already ships against the same repository and propose route. The bidirectional machinery the owner points at is real for the funding plan (create/update/delete with readback verifiers). · ledger: `OS-104`

## 6. RECENT COMMITS IN THIS LINE

- `0585b9a fix(dial): the numbers moved with the selection, because one render path never placed them`
- `067b855 fix(visual): the 104px rail needs 15px type, and the labels 10 units of clearance`
- `03d54e4 docs(OS-111): record D-024 and close the task, on measurement`
- `ec5aa94 fix(visual): three overlaps the owner reported, measured before and after`
- `9f68a50 feat(money): Today and Plan publish ONE Safe-to-Spend figure (OS-111)`
- `1b31933 docs(ledger): settle the goals term -- money sitting in the pocket, never the target`
- `d6381df docs: prepare OS-111 for a fresh session and record the owner's order`
- `4a91bc9 docs(ledger): record the FIRST ACTION for OS-111 and OS-113`
- `1f8cc0a feat(today): Safe to Spend explains how it was reached (OS-079, form B)`
- `d8ade65 fix(safe-to-spend): one spend-pocket rule, so a rename in Crew cannot change the figure`
- `49006fd feat(dial): markers carry miniatures of the icons the event list uses`
- `a14ae9a docs(ledger): record OS-106..OS-110, the evening's visual corrections`

## 7. READ DEEPER — where the facts actually live

- `docs/project/MERIDIAN_ROADMAP.md` — the trajectory and its stated next move
- `docs/project/MERIDIAN_OS_TASKS.json` — every task, its track, gates and limits
- `docs/project/MERIDIAN_DECISIONS.md` — binding decisions; the newest governs
- `docs/project/CURRENT_STATUS.md` — the running record of what was verified
- `docs/project/AGENT_COORDINATION.md` — the claims table — add a row before editing
- `AGENTS.md` — the rules, including the continuity and handoff contracts

**Before acting:** run `scripts/roadmap_handoff_check.py` and reconcile session momentum against project state, per AGENTS.md. If this handoff and the docs disagree, the docs win.
