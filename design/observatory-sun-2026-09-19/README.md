# Observatory sun — 2026-09-19

One built-in `image_gen` call, using `design/observatory-extension-2026-09-18/concepts/timeline.png` as style reference (the Yesterday marker). The owner accepted a sun-only pass to conserve usage; replacing the full generic icon set remains a future goal.

- Asset: `static/img/meridian/observatory/sun-engraving-2026-09-19.png`
- Exact generation prompt: `prompt.txt`; hash, dimensions, alpha and byte count: `asset.json`.
- Newly generated reference-inspired artwork, not an extracted original, not a Bootstrap icon, and not covered by that icon set's license. No claim of exclusive rights is made.
- One output, no image-generation iterations or postprocessing. Real RGBA transparency; 1254×1254; 962,012 bytes. Runtime browser scaling preserves the unmodified master. A smaller delivery export is a future optimization, not performed in this usage-bounded pass.
- Activity uses the PNG as a background on its existing decorative `aria-hidden` sun marker. Masks are disabled only for the sun so engraving and color survive. The marker slot is 24px; Today retains the supplied crescent. The old authored `sun.svg` remains as previous source, untouched.
- The result is an ornate interpretation rather than pixel-identical extraction. It does not include the concept's circular day-marker frame. No other icons have been replaced.
- Governed Activity capture evidence: `artifacts/observatory-sun-2026-09-19/`. `sun-detail-*.png` are supplemental browser close-ups, not parity evidence (no frozen clock in that small asset-load probe).
